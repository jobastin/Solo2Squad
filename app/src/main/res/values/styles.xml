<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/white</item>
    </style>

    <!--Toolbar theme-->
    <style name="CustomToolbarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/white</item>
    </style>

    <!--Menu Item Theme-->
    <style name="CustomPopupStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/white</item>
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-4dp</item>
    </style>

    <!--Input Theme-->
    <style name="CustomInputStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/colorStandardBlack</item>
        <item name="android:theme">@color/colorDarkBlue</item>
    </style>

    <style name="PaymentField">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@android:color/black</item>
        <!-- Add more styling attributes as needed -->
    </style>

    <style name="TextLargeGameSelection">
        <item name="android:textSize">18sp</item>
        <!-- Add other attributes as needed -->
    </style>

    <style name="TextMediumGameSelection">
        <item name="android:textSize">16sp</item>
        <!-- Add other attributes as needed -->
    </style>

    <style name="EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <!-- Add other attributes as needed -->
    </style>

    <style name="ButtonPrimaryGameSelection">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <!-- Add other attributes as needed -->
    </style>

</resources>